# `report` 契约骨架

## 模块目标

定义结构化 JSON 输出、运行元数据和后续报告层的稳定边界。

## 当前实现基线

- `render_news_json`

## 需要固化的契约

### 1. JSON 输出结构

- 顶层结构：
- 结果数组字段：
- 元数据字段：

### 2. 运行上下文

- 是否输出 `RunContext`：
- 版本信息：
- 错误输出格式：

### 3. 报告边界

- 首版保留：
- 首版延后：

## 兼容要求

- 是否要求兼容旧 JSON / HTML 报告结构：
- 不兼容时的替代输出说明：

## 验证方式

- fixture：
- 测试：
- 快照：

## 待补充决策

- `report` 是否同时负责错误渲染
- HTML 报告何时从“延后”进入实现
